# Turntable Object Scan

Template for: depth maps → model → UV → texture → export model.

Run:
- GUI: Tools → Run Script… → `workflows/terrestrial_object_scan_turntable/run.py`
- Headless: `metashape.exe -r workflows/terrestrial_object_scan_turntable/run.py` :contentReference[oaicite:15]{index=15}